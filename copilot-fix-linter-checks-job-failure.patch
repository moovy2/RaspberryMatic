From 08ab8e167b7181c45d2aa2e82c48c62ee0d85994 Mon Sep 17 00:00:00 2001
From: "copilot-swe-agent[bot]" <198982749+Copilot@users.noreply.github.com>
Date: Wed, 16 Sep 2026 18:56:20 +0000
Subject: [PATCH] Rebase stale rootfs patches for OpenCCU-Base 3ed652a

Co-authored-by: jens-maus <5887769+jens-maus@users.noreply.github.com>
---
 .../0077-WebUI-NegateCondition.patch          |  20 ++-
 .../rootfs/www/rega/esp/programs.fn           |  42 +++--
 .../rootfs/www/rega/esp/programs.fn.orig      |  42 +++--
 .../0134-WebUI-Add-ShowInternalSysvars.patch  |  35 ++--
 .../www/rega/pages/tabs/admin/systemvars.htm  |  49 ++++-
 .../rega/pages/tabs/admin/systemvars.htm.orig |  49 ++++-
 ...Add-SettingsAccessInStatusAndControl.patch |   7 -
 .../rega/pages/tabs/admin/views/programs.htm  |   2 +-
 .../pages/tabs/admin/views/programs.htm.orig  |   4 +-
 .../0207-WebUI-Fix-ReGaScriptInjection.patch  |  10 --
 .../rootfs/www/api/eq3/hmscript.tcl           | 167 ++++++++++--------
 .../rootfs/www/api/eq3/hmscript.tcl.orig      | 166 ++++++++++-------
 12 files changed, 367 insertions(+), 226 deletions(-)

diff --git a/buildroot-external/package/openccu-base/rootfs-patches/0077-WebUI-NegateCondition.patch b/buildroot-external/package/openccu-base/rootfs-patches/0077-WebUI-NegateCondition.patch
index 263ee2c..a7af8e9 100644
--- a/buildroot-external/package/openccu-base/rootfs-patches/0077-WebUI-NegateCondition.patch
+++ b/buildroot-external/package/openccu-base/rootfs-patches/0077-WebUI-NegateCondition.patch
@@ -11,17 +11,21 @@
                    }
  
                    !SPHM-934
-@@ -83,11 +84,11 @@
-                     {
-                       if( iRV1 == 0 )
-                       {
--                        sRet = sRet#', ${ruleConditionLblWhen} '#oLV.ValueName0()#', ';
-+                        sRet = sRet#' '#oLV.ValueName0()#' ';
+@@ -87,7 +88,7 @@
+                         sVN0 = sVN0.Replace("&", "&amp;");
+                         sVN0 = sVN0.Replace("<", "&lt;");
+                         sVN0 = sVN0.Replace(">", "&gt;");
+-                        sRet = sRet#', ${ruleConditionLblWhen} '#sVN0#', ';
++                        sRet = sRet#' '#sVN0#' ';
                        }
                        if( iRV1 == 1 )
                        {
--                        sRet = sRet#', ${ruleConditionLblWhen} '#oLV.ValueName1()#', ';
-+                        sRet = sRet#' '#oLV.ValueName1()#' ';
+@@ -95,7 +96,7 @@
+                         sVN1 = sVN1.Replace("&", "&amp;");
+                         sVN1 = sVN1.Replace("<", "&lt;");
+                         sVN1 = sVN1.Replace(">", "&gt;");
+-                        sRet = sRet#', ${ruleConditionLblWhen} '#sVN1#', ';
++                        sRet = sRet#' '#sVN1#' ';
                        }
                      }
                    }
diff --git a/buildroot-external/package/openccu-base/rootfs-patches/0077-WebUI-NegateCondition/rootfs/www/rega/esp/programs.fn b/buildroot-external/package/openccu-base/rootfs-patches/0077-WebUI-NegateCondition/rootfs/www/rega/esp/programs.fn
index 60ffdca..30683e2 100644
--- a/buildroot-external/package/openccu-base/rootfs-patches/0077-WebUI-NegateCondition/rootfs/www/rega/esp/programs.fn
+++ b/buildroot-external/package/openccu-base/rootfs-patches/0077-WebUI-NegateCondition/rootfs/www/rega/esp/programs.fn
@@ -50,7 +50,7 @@ function ::WriteConditionText()
                   }
                   else
                   {
-                    sRet = sRet#" (nicht ausgewählt)";
+                    sRet = sRet#" (nicht ausgewï¿½hlt)";
                   }
                 }
                 if( iLVT == ivtSystemId )
@@ -62,7 +62,7 @@ function ::WriteConditionText()
                   }
                   else
                   {
-                    sRet = sRet#" (nicht ausgewählt)";
+                    sRet = sRet#" (nicht ausgewï¿½hlt)";
                   }
                 }
                 if( bOK )
@@ -84,11 +84,19 @@ function ::WriteConditionText()
                     {
                       if( iRV1 == 0 )
                       {
-                        sRet = sRet#' '#oLV.ValueName0()#' ';
+                        string sVN0 = oLV.ValueName0();
+                        sVN0 = sVN0.Replace("&", "&amp;");
+                        sVN0 = sVN0.Replace("<", "&lt;");
+                        sVN0 = sVN0.Replace(">", "&gt;");
+                        sRet = sRet#' '#sVN0#' ';
                       }
                       if( iRV1 == 1 )
                       {
-                        sRet = sRet#' '#oLV.ValueName1()#' ';
+                        string sVN1 = oLV.ValueName1();
+                        sVN1 = sVN1.Replace("&", "&amp;");
+                        sVN1 = sVN1.Replace("<", "&lt;");
+                        sVN1 = sVN1.Replace(">", "&gt;");
+                        sRet = sRet#' '#sVN1#' ';
                       }
                     }
                   }
@@ -196,7 +204,7 @@ function ::WriteConditionText()
                                   Call("/esp/functions.fn::getSpecialBinaryTranslation()");
                                   sRet = sRet#' '#sValue;
                                 }
-                                !Write( "<script>conInfo('- ist binär und Action, RV1="#iRV1#" -- RV2="#iRV2#"');</script>" );
+                                !Write( "<script>conInfo('- ist binï¿½r und Action, RV1="#iRV1#" -- RV2="#iRV2#"');</script>" );
                               }
                               else
                               {
@@ -278,9 +286,9 @@ function ::WriteConditionText()
                       iRV2 = iRV2*100;
                     }
                     
-                    if( sValueUnit == "°C" )
+                    if( sValueUnit == "ï¿½C" )
                     {
-                      sValueUnit = "° C";
+                      sValueUnit = "ï¿½ C";
                     }
                     else
                     {
@@ -306,11 +314,11 @@ function ::WriteConditionText()
                         sCT = "${ruleConditionLblRangeFrom} "#iRV1.ToString(2)#sValueUnit#" ${ruleConditionLblRangeGreaterThan} "#iRV2.ToString(2)#sValueUnit;
                       }
                     }
-                    !if( iCT1 == 8 ) { sCT = "größer als "#iRV1.ToString(2)#sValueUnit; }
+                    !if( iCT1 == 8 ) { sCT = "grï¿½ï¿½er als "#iRV1.ToString(2)#sValueUnit; }
                     if( iCT1 == 8 ) { sCT = "${ruleConditionLblGreaterThan} "#iRV1.ToString(2)#sValueUnit; }
                     !if( iCT1 == 10 ) { sCT = "kleiner als "#iRV1.ToString(2)#sValueUnit; }
                     if( iCT1 == 10 ) { sCT = "${ruleConditionLblLessThan} "#iRV1.ToString(2)#sValueUnit; }
-                    !if( iCT1 == 9 ) { sCT = "größer oder gleich "#iRV1.ToString(2)#sValueUnit; }
+                    !if( iCT1 == 9 ) { sCT = "grï¿½ï¿½er oder gleich "#iRV1.ToString(2)#sValueUnit; }
                     if( iCT1 == 9 ) { sCT = "${ruleConditionLblGreaterOrEqualThan} "#iRV1.ToString(2)#sValueUnit; }
                     !if( iCT1 == 11 ) { sCT = "kleiner oder gleich "#iRV1.ToString(2)#sValueUnit; }
                     if( iCT1 == 11 ) { sCT = "${ruleConditionLblLessOrEqualThan} "#iRV1.ToString(2)#sValueUnit; }
@@ -328,11 +336,11 @@ function ::WriteConditionText()
                     {
                       oSC.ConditionType2(4);
                     }
-                    !if( iCT2 == 13 ) {sCT = 'auslösen auf Aktualisierung';}
+                    !if( iCT2 == 13 ) {sCT = 'auslï¿½sen auf Aktualisierung';}
                     if( iCT2 == 13 ) {sCT = '${ruleConditionSelectTriggerWhenUpdated}';}
-                    !if( iCT2 == 15 ) {sCT = 'nur prüfen';}
+                    !if( iCT2 == 15 ) {sCT = 'nur prï¿½fen';}
                     if( iCT2 == 15 ) {sCT = '${ruleConditionSelectCheckOnly}';}
-                    !if( iCT2 == 4 ) {sCT = 'auslösen auf Änderung';}
+                    !if( iCT2 == 4 ) {sCT = 'auslï¿½sen auf ï¿½nderung';}
                     if( iCT2 == 4 ) {sCT = '${ruleConditionSelectWhenChanged}';}
                     sRet = sRet#' '#sCT;
                   }
@@ -383,9 +391,9 @@ function ::WriteConditionText()
                   {
                     oSC.ConditionType2(13);
                   }
-                  !if( iCT2 == 13 ) {sRet = sRet#' auslösen zu Zeitpunkten';}
+                  !if( iCT2 == 13 ) {sRet = sRet#' auslï¿½sen zu Zeitpunkten';}
                   if( iCT2 == 13 ) {sRet = sRet#' ${ruleConditionSelectTriggerOnTime}';}
-                  !if( iCT2 == 15 ) {sRet = sRet#' nur prüfen';}
+                  !if( iCT2 == 15 ) {sRet = sRet#' nur prï¿½fen';}
                   if( iCT2 == 15 ) {sRet = sRet#' ${ruleConditionSelectCheckOnly}';}
                 }
                 else
@@ -464,7 +472,7 @@ function ::WriteDestinationText()
             {
               if( iVPT == ivtDelay )
               {
-                !sRet = sRet#" verzögert um " ;
+                !sRet = sRet#" verzï¿½gert um " ;
                 sRet = sRet#" ${ruleActivitySelectDelayed} " ;
                 integer iHours = iVP.ToString().Substr(11,2).ToInteger();
                 integer iMinutes = iVP.ToString().Substr(14,2).ToInteger();
@@ -489,7 +497,7 @@ function ::WriteDestinationText()
               ! ##############################################################################
               if( iP == ivtString )
               {
-                !sRet = sRet#" ausführen";
+                !sRet = sRet#" ausfï¿½hren";
                 sRet = sRet#" ${ruleDescrPerform}";
               }
               if( iP == ivtObjectId )
@@ -721,7 +729,7 @@ function ::WriteDestinationText()
         }
         else
         {
-          !sRet = sRet#"Keine Aktivitäten";
+          !sRet = sRet#"Keine Aktivitï¿½ten";
           sRet = sRet#"${ruleDescrNoActivities}";
         }
       }
diff --git a/buildroot-external/package/openccu-base/rootfs-patches/0077-WebUI-NegateCondition/rootfs/www/rega/esp/programs.fn.orig b/buildroot-external/package/openccu-base/rootfs-patches/0077-WebUI-NegateCondition/rootfs/www/rega/esp/programs.fn.orig
index 4cf67d9..214d883 100644
--- a/buildroot-external/package/openccu-base/rootfs-patches/0077-WebUI-NegateCondition/rootfs/www/rega/esp/programs.fn.orig
+++ b/buildroot-external/package/openccu-base/rootfs-patches/0077-WebUI-NegateCondition/rootfs/www/rega/esp/programs.fn.orig
@@ -50,7 +50,7 @@ function ::WriteConditionText()
                   }
                   else
                   {
-                    sRet = sRet#" (nicht ausgewählt)";
+                    sRet = sRet#" (nicht ausgewï¿½hlt)";
                   }
                 }
                 if( iLVT == ivtSystemId )
@@ -62,7 +62,7 @@ function ::WriteConditionText()
                   }
                   else
                   {
-                    sRet = sRet#" (nicht ausgewählt)";
+                    sRet = sRet#" (nicht ausgewï¿½hlt)";
                   }
                 }
                 if( bOK )
@@ -83,11 +83,19 @@ function ::WriteConditionText()
                     {
                       if( iRV1 == 0 )
                       {
-                        sRet = sRet#', ${ruleConditionLblWhen} '#oLV.ValueName0()#', ';
+                        string sVN0 = oLV.ValueName0();
+                        sVN0 = sVN0.Replace("&", "&amp;");
+                        sVN0 = sVN0.Replace("<", "&lt;");
+                        sVN0 = sVN0.Replace(">", "&gt;");
+                        sRet = sRet#', ${ruleConditionLblWhen} '#sVN0#', ';
                       }
                       if( iRV1 == 1 )
                       {
-                        sRet = sRet#', ${ruleConditionLblWhen} '#oLV.ValueName1()#', ';
+                        string sVN1 = oLV.ValueName1();
+                        sVN1 = sVN1.Replace("&", "&amp;");
+                        sVN1 = sVN1.Replace("<", "&lt;");
+                        sVN1 = sVN1.Replace(">", "&gt;");
+                        sRet = sRet#', ${ruleConditionLblWhen} '#sVN1#', ';
                       }
                     }
                   }
@@ -195,7 +203,7 @@ function ::WriteConditionText()
                                   Call("/esp/functions.fn::getSpecialBinaryTranslation()");
                                   sRet = sRet#' '#sValue;
                                 }
-                                !Write( "<script>conInfo('- ist binär und Action, RV1="#iRV1#" -- RV2="#iRV2#"');</script>" );
+                                !Write( "<script>conInfo('- ist binï¿½r und Action, RV1="#iRV1#" -- RV2="#iRV2#"');</script>" );
                               }
                               else
                               {
@@ -277,9 +285,9 @@ function ::WriteConditionText()
                       iRV2 = iRV2*100;
                     }
                     
-                    if( sValueUnit == "°C" )
+                    if( sValueUnit == "ï¿½C" )
                     {
-                      sValueUnit = "° C";
+                      sValueUnit = "ï¿½ C";
                     }
                     else
                     {
@@ -305,11 +313,11 @@ function ::WriteConditionText()
                         sCT = "${ruleConditionLblRangeFrom} "#iRV1.ToString(2)#sValueUnit#" ${ruleConditionLblRangeGreaterThan} "#iRV2.ToString(2)#sValueUnit;
                       }
                     }
-                    !if( iCT1 == 8 ) { sCT = "größer als "#iRV1.ToString(2)#sValueUnit; }
+                    !if( iCT1 == 8 ) { sCT = "grï¿½ï¿½er als "#iRV1.ToString(2)#sValueUnit; }
                     if( iCT1 == 8 ) { sCT = "${ruleConditionLblGreaterThan} "#iRV1.ToString(2)#sValueUnit; }
                     !if( iCT1 == 10 ) { sCT = "kleiner als "#iRV1.ToString(2)#sValueUnit; }
                     if( iCT1 == 10 ) { sCT = "${ruleConditionLblLessThan} "#iRV1.ToString(2)#sValueUnit; }
-                    !if( iCT1 == 9 ) { sCT = "größer oder gleich "#iRV1.ToString(2)#sValueUnit; }
+                    !if( iCT1 == 9 ) { sCT = "grï¿½ï¿½er oder gleich "#iRV1.ToString(2)#sValueUnit; }
                     if( iCT1 == 9 ) { sCT = "${ruleConditionLblGreaterOrEqualThan} "#iRV1.ToString(2)#sValueUnit; }
                     !if( iCT1 == 11 ) { sCT = "kleiner oder gleich "#iRV1.ToString(2)#sValueUnit; }
                     if( iCT1 == 11 ) { sCT = "${ruleConditionLblLessOrEqualThan} "#iRV1.ToString(2)#sValueUnit; }
@@ -327,11 +335,11 @@ function ::WriteConditionText()
                     {
                       oSC.ConditionType2(4);
                     }
-                    !if( iCT2 == 13 ) {sCT = 'auslösen auf Aktualisierung';}
+                    !if( iCT2 == 13 ) {sCT = 'auslï¿½sen auf Aktualisierung';}
                     if( iCT2 == 13 ) {sCT = '${ruleConditionSelectTriggerWhenUpdated}';}
-                    !if( iCT2 == 15 ) {sCT = 'nur prüfen';}
+                    !if( iCT2 == 15 ) {sCT = 'nur prï¿½fen';}
                     if( iCT2 == 15 ) {sCT = '${ruleConditionSelectCheckOnly}';}
-                    !if( iCT2 == 4 ) {sCT = 'auslösen auf Änderung';}
+                    !if( iCT2 == 4 ) {sCT = 'auslï¿½sen auf ï¿½nderung';}
                     if( iCT2 == 4 ) {sCT = '${ruleConditionSelectWhenChanged}';}
                     sRet = sRet#' '#sCT;
                   }
@@ -382,9 +390,9 @@ function ::WriteConditionText()
                   {
                     oSC.ConditionType2(13);
                   }
-                  !if( iCT2 == 13 ) {sRet = sRet#' auslösen zu Zeitpunkten';}
+                  !if( iCT2 == 13 ) {sRet = sRet#' auslï¿½sen zu Zeitpunkten';}
                   if( iCT2 == 13 ) {sRet = sRet#' ${ruleConditionSelectTriggerOnTime}';}
-                  !if( iCT2 == 15 ) {sRet = sRet#' nur prüfen';}
+                  !if( iCT2 == 15 ) {sRet = sRet#' nur prï¿½fen';}
                   if( iCT2 == 15 ) {sRet = sRet#' ${ruleConditionSelectCheckOnly}';}
                 }
                 else
@@ -463,7 +471,7 @@ function ::WriteDestinationText()
             {
               if( iVPT == ivtDelay )
               {
-                !sRet = sRet#" verzögert um " ;
+                !sRet = sRet#" verzï¿½gert um " ;
                 sRet = sRet#" ${ruleActivitySelectDelayed} " ;
                 integer iHours = iVP.ToString().Substr(11,2).ToInteger();
                 integer iMinutes = iVP.ToString().Substr(14,2).ToInteger();
@@ -488,7 +496,7 @@ function ::WriteDestinationText()
               ! ##############################################################################
               if( iP == ivtString )
               {
-                !sRet = sRet#" ausführen";
+                !sRet = sRet#" ausfï¿½hren";
                 sRet = sRet#" ${ruleDescrPerform}";
               }
               if( iP == ivtObjectId )
@@ -720,7 +728,7 @@ function ::WriteDestinationText()
         }
         else
         {
-          !sRet = sRet#"Keine Aktivitäten";
+          !sRet = sRet#"Keine Aktivitï¿½ten";
           sRet = sRet#"${ruleDescrNoActivities}";
         }
       }
diff --git a/buildroot-external/package/openccu-base/rootfs-patches/0134-WebUI-Add-ShowInternalSysvars.patch b/buildroot-external/package/openccu-base/rootfs-patches/0134-WebUI-Add-ShowInternalSysvars.patch
index f0e230b..d2cdb44 100644
--- a/buildroot-external/package/openccu-base/rootfs-patches/0134-WebUI-Add-ShowInternalSysvars.patch
+++ b/buildroot-external/package/openccu-base/rootfs-patches/0134-WebUI-Add-ShowInternalSysvars.patch
@@ -112,20 +112,29 @@
  <colgroup>
    <col />
    <col />
-@@ -141,15 +153,17 @@
+@@ -141,8 +153,10 @@
      string sTrId = "sv"#sysvar;
      Write("<tr id='"#sTrId#"'>");
      object sv = dom.GetObject(sysvar);
--    Write("<td style='text-align:center;' class='GrayBkg CLASS05206' >");
 +    string CellBkg = "GrayBkg";
 +    if (sv.Internal() == true) { CellBkg = "LightGrayBkg"; }
+     string sHtml;
+-    Write("<td style='text-align:center;' class='GrayBkg CLASS05206' >");
 +    Write("<td style='text-align:center;' class='"#CellBkg#" CLASS05206' >");
-       Write(sv.Name());
+       sHtml = sv.Name();
+       sHtml = sHtml.Replace("&", "&amp;");
+       sHtml = sHtml.Replace("<", "&lt;");
+@@ -150,7 +164,7 @@
+       Write(sHtml);
      Write("</td>");
      
 -    Write("<td  style='text-align:center;' class='GrayBkg CLASS05206' >");
 +    Write("<td  style='text-align:center;' class='"#CellBkg#" CLASS05206' >");
-       Write(sv.DPInfo().Replace('\r\n', '<br/>').Replace('\r', '<br/>').Replace('\n', '<br/>'));
+       sHtml = sv.DPInfo().Replace("\r\n", "<br/>").Replace("\r", "<br/>").Replace("\n", "<br/>");
+       sHtml = sHtml.Replace("&", "&amp;");
+       sHtml = sHtml.Replace("<", "&lt;");
+@@ -158,7 +172,7 @@
+       Write(sHtml);
      Write("</td>");
      
 -    Write("<td  style='text-align:center;' class='GrayBkg CLASS05206' >");
@@ -133,7 +142,7 @@
        string s = "Unbekannt: " # sv.ValueSubType();
        if( sv.ValueSubType() == istPresent )
        {
-@@ -178,7 +192,7 @@
+@@ -187,7 +201,7 @@
        Write(s);
      Write("</td>");
      
@@ -142,13 +151,17 @@
        if (sv.ValueSubType() == istPresent) {
        }
        if (sv.ValueSubType() == istChar8859) {
-@@ -202,19 +216,21 @@
+@@ -231,7 +245,7 @@
      Write("</td>");
      
      
 -    Write("<td class='GrayBkg CLASS05206' >");
 +    Write("<td class='"#CellBkg#" CLASS05206' >");
-       Write(sv.ValueUnit());
+       sHtml = sv.ValueUnit();
+       sHtml = sHtml.Replace("&", "&amp;");
+       sHtml = sHtml.Replace("<", "&lt;");
+@@ -239,15 +253,17 @@
+       Write(sHtml);
      Write("</td>");
      
 -    Write("<td class='GrayBkg CLASS05206' >");
@@ -168,7 +181,7 @@
          Write("<td class='CLASS05210'>");
            string sClassName = "StdButton";
            string sOnclick = "DeleteSysVar("#sv.ID()#");";
-@@ -225,6 +241,22 @@
+@@ -258,6 +274,22 @@
            }
            Write("<div onclick='"#sOnclick#"' class='"#sClassName#"'>${btnRemove}</div>");
          Write("</td>");
@@ -191,7 +204,7 @@
          Write("<td class='CLASS05211'>");
            string sChecked = "";
            if (sv.Visible()) {
-@@ -232,24 +264,29 @@
+@@ -265,24 +297,29 @@
            }
            Write("<input id='svvis"#sv.ID()#"' type='checkbox' "#sChecked#" onclick='iseSystem.saveDpVisibility("#sv.ID()#", this)' />${lblVisible}");
          Write("</td>");
@@ -229,7 +242,7 @@
        string sJSTmp = "WebUI.enter(ProgramListPage, '"#sv.DPEnumUsagePrograms()#"');";
        Write( '<div class="StdButton" onclick="'#sJSTmp#'">${btnPrograms}</div>' );
      Write("</td>");
-@@ -305,7 +342,39 @@
+@@ -338,7 +375,39 @@
    new iseSubMenuControl("btnFilterVarType", "btnFilterVarTypeSub", "svOffset", {}, mouseOpts, false, true);  
    new iseSubMenuControl("btnFilterUnit", "btnFilterUnitSub", "svOffset", {}, mouseOpts, false, true);
    new iseSubMenuControl("btnFilterChn", "btnFilterChnSub", "svOffset", {}, mouseOpts, false, true);
@@ -270,7 +283,7 @@
    var s = "";
    
    s += "<table cellspacing='8'>";
-@@ -313,6 +382,7 @@
+@@ -346,6 +415,7 @@
      s += "<td  style='text-align:center; vertical-align: middle;'><div class='FooterButton' onclick='WebUI.goBack()'>${footerBtnPageBack}</div></td>";
      s += "<td  style='text-align:center; vertical-align: middle;'><div class='FooterButton CLASS05207' onclick='fltSYV.clearFilters();reloadPage();' >${footerBtnResetFilter}</div></td>";
      s += "<td  style='text-align:center; vertical-align: middle;'><div class='FooterButton' onclick='newSysVar()'>${footerBtnNew}</div></td>";
diff --git a/buildroot-external/package/openccu-base/rootfs-patches/0134-WebUI-Add-ShowInternalSysvars/rootfs/www/rega/pages/tabs/admin/systemvars.htm b/buildroot-external/package/openccu-base/rootfs-patches/0134-WebUI-Add-ShowInternalSysvars/rootfs/www/rega/pages/tabs/admin/systemvars.htm
index df1636a..f1e1432 100644
--- a/buildroot-external/package/openccu-base/rootfs-patches/0134-WebUI-Add-ShowInternalSysvars/rootfs/www/rega/pages/tabs/admin/systemvars.htm
+++ b/buildroot-external/package/openccu-base/rootfs-patches/0134-WebUI-Add-ShowInternalSysvars/rootfs/www/rega/pages/tabs/admin/systemvars.htm
@@ -155,12 +155,21 @@
     object sv = dom.GetObject(sysvar);
     string CellBkg = "GrayBkg";
     if (sv.Internal() == true) { CellBkg = "LightGrayBkg"; }
+    string sHtml;
     Write("<td style='text-align:center;' class='"#CellBkg#" CLASS05206' >");
-      Write(sv.Name());
+      sHtml = sv.Name();
+      sHtml = sHtml.Replace("&", "&amp;");
+      sHtml = sHtml.Replace("<", "&lt;");
+      sHtml = sHtml.Replace(">", "&gt;");
+      Write(sHtml);
     Write("</td>");
     
     Write("<td  style='text-align:center;' class='"#CellBkg#" CLASS05206' >");
-      Write(sv.DPInfo().Replace('\r\n', '<br/>').Replace('\r', '<br/>').Replace('\n', '<br/>'));
+      sHtml = sv.DPInfo().Replace("\r\n", "<br/>").Replace("\r", "<br/>").Replace("\n", "<br/>");
+      sHtml = sHtml.Replace("&", "&amp;");
+      sHtml = sHtml.Replace("<", "&lt;");
+      sHtml = sHtml.Replace(">", "&gt;");
+      Write(sHtml);
     Write("</td>");
     
     Write("<td  style='text-align:center;' class='"#CellBkg#" CLASS05206' >");
@@ -198,26 +207,50 @@
       if (sv.ValueSubType() == istChar8859) {
       }
       if (sv.ValueSubType() == istAlarm) {
-        Write("${lblTrue} = " # sv.ValueName1()#"<br />");
-        Write("${lblFalse} = " # sv.ValueName0());
+        sHtml = sv.ValueName1();
+        sHtml = sHtml.Replace("&", "&amp;");
+        sHtml = sHtml.Replace("<", "&lt;");
+        sHtml = sHtml.Replace(">", "&gt;");
+        Write("${lblTrue} = " # sHtml#"<br />");
+        sHtml = sv.ValueName0();
+        sHtml = sHtml.Replace("&", "&amp;");
+        sHtml = sHtml.Replace("<", "&lt;");
+        sHtml = sHtml.Replace(">", "&gt;");
+        Write("${lblFalse} = " # sHtml);
       }
       if (sv.ValueSubType() == istGeneric) {
         Write("${lblMinValue}: " # sv.ValueMin()#"<br />");
         Write("${lblMaxValue}: " # sv.ValueMax());
       }
       if (sv.ValueSubType() == istEnum) {
-        Write(sv.ValueList());
+        sHtml = sv.ValueList();
+        sHtml = sHtml.Replace("&", "&amp;");
+        sHtml = sHtml.Replace("<", "&lt;");
+        sHtml = sHtml.Replace(">", "&gt;");
+        Write(sHtml);
       }
       if (sv.ValueSubType() == istBool) {
-        Write("${lblTrue} = " # sv.ValueName1()#"<br />");
-        Write("${lblFalse} = " # sv.ValueName0());
+        sHtml = sv.ValueName1();
+        sHtml = sHtml.Replace("&", "&amp;");
+        sHtml = sHtml.Replace("<", "&lt;");
+        sHtml = sHtml.Replace(">", "&gt;");
+        Write("${lblTrue} = " # sHtml#"<br />");
+        sHtml = sv.ValueName0();
+        sHtml = sHtml.Replace("&", "&amp;");
+        sHtml = sHtml.Replace("<", "&lt;");
+        sHtml = sHtml.Replace(">", "&gt;");
+        Write("${lblFalse} = " # sHtml);
       }
       
     Write("</td>");
     
     
     Write("<td class='"#CellBkg#" CLASS05206' >");
-      Write(sv.ValueUnit());
+      sHtml = sv.ValueUnit();
+      sHtml = sHtml.Replace("&", "&amp;");
+      sHtml = sHtml.Replace("<", "&lt;");
+      sHtml = sHtml.Replace(">", "&gt;");
+      Write(sHtml);
     Write("</td>");
     
     Write("<td class='"#CellBkg#" CLASS05206' >");
diff --git a/buildroot-external/package/openccu-base/rootfs-patches/0134-WebUI-Add-ShowInternalSysvars/rootfs/www/rega/pages/tabs/admin/systemvars.htm.orig b/buildroot-external/package/openccu-base/rootfs-patches/0134-WebUI-Add-ShowInternalSysvars/rootfs/www/rega/pages/tabs/admin/systemvars.htm.orig
index 5b1afde..c19071b 100644
--- a/buildroot-external/package/openccu-base/rootfs-patches/0134-WebUI-Add-ShowInternalSysvars/rootfs/www/rega/pages/tabs/admin/systemvars.htm.orig
+++ b/buildroot-external/package/openccu-base/rootfs-patches/0134-WebUI-Add-ShowInternalSysvars/rootfs/www/rega/pages/tabs/admin/systemvars.htm.orig
@@ -141,12 +141,21 @@
     string sTrId = "sv"#sysvar;
     Write("<tr id='"#sTrId#"'>");
     object sv = dom.GetObject(sysvar);
+    string sHtml;
     Write("<td style='text-align:center;' class='GrayBkg CLASS05206' >");
-      Write(sv.Name());
+      sHtml = sv.Name();
+      sHtml = sHtml.Replace("&", "&amp;");
+      sHtml = sHtml.Replace("<", "&lt;");
+      sHtml = sHtml.Replace(">", "&gt;");
+      Write(sHtml);
     Write("</td>");
     
     Write("<td  style='text-align:center;' class='GrayBkg CLASS05206' >");
-      Write(sv.DPInfo().Replace('\r\n', '<br/>').Replace('\r', '<br/>').Replace('\n', '<br/>'));
+      sHtml = sv.DPInfo().Replace("\r\n", "<br/>").Replace("\r", "<br/>").Replace("\n", "<br/>");
+      sHtml = sHtml.Replace("&", "&amp;");
+      sHtml = sHtml.Replace("<", "&lt;");
+      sHtml = sHtml.Replace(">", "&gt;");
+      Write(sHtml);
     Write("</td>");
     
     Write("<td  style='text-align:center;' class='GrayBkg CLASS05206' >");
@@ -184,26 +193,50 @@
       if (sv.ValueSubType() == istChar8859) {
       }
       if (sv.ValueSubType() == istAlarm) {
-        Write("${lblTrue} = " # sv.ValueName1()#"<br />");
-        Write("${lblFalse} = " # sv.ValueName0());
+        sHtml = sv.ValueName1();
+        sHtml = sHtml.Replace("&", "&amp;");
+        sHtml = sHtml.Replace("<", "&lt;");
+        sHtml = sHtml.Replace(">", "&gt;");
+        Write("${lblTrue} = " # sHtml#"<br />");
+        sHtml = sv.ValueName0();
+        sHtml = sHtml.Replace("&", "&amp;");
+        sHtml = sHtml.Replace("<", "&lt;");
+        sHtml = sHtml.Replace(">", "&gt;");
+        Write("${lblFalse} = " # sHtml);
       }
       if (sv.ValueSubType() == istGeneric) {
         Write("${lblMinValue}: " # sv.ValueMin()#"<br />");
         Write("${lblMaxValue}: " # sv.ValueMax());
       }
       if (sv.ValueSubType() == istEnum) {
-        Write(sv.ValueList());
+        sHtml = sv.ValueList();
+        sHtml = sHtml.Replace("&", "&amp;");
+        sHtml = sHtml.Replace("<", "&lt;");
+        sHtml = sHtml.Replace(">", "&gt;");
+        Write(sHtml);
       }
       if (sv.ValueSubType() == istBool) {
-        Write("${lblTrue} = " # sv.ValueName1()#"<br />");
-        Write("${lblFalse} = " # sv.ValueName0());
+        sHtml = sv.ValueName1();
+        sHtml = sHtml.Replace("&", "&amp;");
+        sHtml = sHtml.Replace("<", "&lt;");
+        sHtml = sHtml.Replace(">", "&gt;");
+        Write("${lblTrue} = " # sHtml#"<br />");
+        sHtml = sv.ValueName0();
+        sHtml = sHtml.Replace("&", "&amp;");
+        sHtml = sHtml.Replace("<", "&lt;");
+        sHtml = sHtml.Replace(">", "&gt;");
+        Write("${lblFalse} = " # sHtml);
       }
       
     Write("</td>");
     
     
     Write("<td class='GrayBkg CLASS05206' >");
-      Write(sv.ValueUnit());
+      sHtml = sv.ValueUnit();
+      sHtml = sHtml.Replace("&", "&amp;");
+      sHtml = sHtml.Replace("<", "&lt;");
+      sHtml = sHtml.Replace(">", "&gt;");
+      Write(sHtml);
     Write("</td>");
     
     Write("<td class='GrayBkg CLASS05206' >");
diff --git a/buildroot-external/package/openccu-base/rootfs-patches/0138-WebUI-Add-SettingsAccessInStatusAndControl.patch b/buildroot-external/package/openccu-base/rootfs-patches/0138-WebUI-Add-SettingsAccessInStatusAndControl.patch
index a09cad0..4e446f3 100644
--- a/buildroot-external/package/openccu-base/rootfs-patches/0138-WebUI-Add-SettingsAccessInStatusAndControl.patch
+++ b/buildroot-external/package/openccu-base/rootfs-patches/0138-WebUI-Add-SettingsAccessInStatusAndControl.patch
@@ -71,13 +71,6 @@
      s += "<td style='text-align:center; vertical-align:middle;'><div class='FooterButton' onclick='SaveProgramEdit(<% Write(oOrigPrg.ID()); %>,<% Write(oPrg.ID()); %>);'>${footerBtnOk}</div></td>";
      s += "<td style='text-align:center; vertical-align:middle;'><div class='FooterButton CLASS04800' onclick='CopyToNewProgram(<% Write(oPrg.ID()); %>);' >${footerBtnSaveNewProgram}</div></td>";
      s += "<td style='text-align:center; vertical-align:middle;'><div class='FooterButton CLASS04801' onclick='new HMScriptExecutor();'>${footerBtnTestScript}</div></td>";
-@@ -1165,4 +1166,4 @@
-   
- </div>
- 
--<script>translatePage('#ProgramTable')</script>
-\ No newline at end of file
-+<script>translatePage('#ProgramTable')</script>
 --- a/www/rega/pages/tabs/control/devices.htm
 +++ b/www/rega/pages/tabs/control/devices.htm
 @@ -90,7 +90,7 @@
diff --git a/buildroot-external/package/openccu-base/rootfs-patches/0138-WebUI-Add-SettingsAccessInStatusAndControl/rootfs/www/rega/pages/tabs/admin/views/programs.htm b/buildroot-external/package/openccu-base/rootfs-patches/0138-WebUI-Add-SettingsAccessInStatusAndControl/rootfs/www/rega/pages/tabs/admin/views/programs.htm
index 3162d09..79b9f10 100644
--- a/buildroot-external/package/openccu-base/rootfs-patches/0138-WebUI-Add-SettingsAccessInStatusAndControl/rootfs/www/rega/pages/tabs/admin/views/programs.htm
+++ b/buildroot-external/package/openccu-base/rootfs-patches/0138-WebUI-Add-SettingsAccessInStatusAndControl/rootfs/www/rega/pages/tabs/admin/views/programs.htm
@@ -810,7 +810,7 @@
       jQuery.each(jElm, function (index, elm) {
         var text = jQuery(elm).find("option:selected").text();
         // create test element
-        var _test = jQuery("<span>").html(text).css({
+        var _test = jQuery("<span>").text(text).css({
           "font-size": jQuery(elm).css("font-size"), // ensures same size text
           "visibility": "hidden"               // prevents FOUC
         });
diff --git a/buildroot-external/package/openccu-base/rootfs-patches/0138-WebUI-Add-SettingsAccessInStatusAndControl/rootfs/www/rega/pages/tabs/admin/views/programs.htm.orig b/buildroot-external/package/openccu-base/rootfs-patches/0138-WebUI-Add-SettingsAccessInStatusAndControl/rootfs/www/rega/pages/tabs/admin/views/programs.htm.orig
index 15d5cee..031b222 100644
--- a/buildroot-external/package/openccu-base/rootfs-patches/0138-WebUI-Add-SettingsAccessInStatusAndControl/rootfs/www/rega/pages/tabs/admin/views/programs.htm.orig
+++ b/buildroot-external/package/openccu-base/rootfs-patches/0138-WebUI-Add-SettingsAccessInStatusAndControl/rootfs/www/rega/pages/tabs/admin/views/programs.htm.orig
@@ -809,7 +809,7 @@
       jQuery.each(jElm, function (index, elm) {
         var text = jQuery(elm).find("option:selected").text();
         // create test element
-        var _test = jQuery("<span>").html(text).css({
+        var _test = jQuery("<span>").text(text).css({
           "font-size": jQuery(elm).css("font-size"), // ensures same size text
           "visibility": "hidden"               // prevents FOUC
         });
@@ -1165,4 +1165,4 @@
   
 </div>
 
-<script>translatePage('#ProgramTable')</script>
\ No newline at end of file
+<script>translatePage('#ProgramTable')</script>
diff --git a/buildroot-external/package/openccu-base/rootfs-patches/0207-WebUI-Fix-ReGaScriptInjection.patch b/buildroot-external/package/openccu-base/rootfs-patches/0207-WebUI-Fix-ReGaScriptInjection.patch
index f84dccf..f1b2ceb 100644
--- a/buildroot-external/package/openccu-base/rootfs-patches/0207-WebUI-Fix-ReGaScriptInjection.patch
+++ b/buildroot-external/package/openccu-base/rootfs-patches/0207-WebUI-Fix-ReGaScriptInjection.patch
@@ -1,13 +1,3 @@
---- a/www/api/eq3/hmscript.tcl
-+++ b/www/api/eq3/hmscript.tcl
-@@ -59,6 +59,7 @@
- 
- proc hmscript_escapeString { str } {
-   return [string map {
-+    "\\" "\\\\"
-     "\'" "\\\'"
-     "\"" "\\\""
-     "\n" "\\n"
 --- a/www/pda/fav.cgi
 +++ b/www/pda/fav.cgi
 @@ -21,7 +21,7 @@
diff --git a/buildroot-external/package/openccu-base/rootfs-patches/0207-WebUI-Fix-ReGaScriptInjection/rootfs/www/api/eq3/hmscript.tcl b/buildroot-external/package/openccu-base/rootfs-patches/0207-WebUI-Fix-ReGaScriptInjection/rootfs/www/api/eq3/hmscript.tcl
index 91f51eb..613cc8a 100644
--- a/buildroot-external/package/openccu-base/rootfs-patches/0207-WebUI-Fix-ReGaScriptInjection/rootfs/www/api/eq3/hmscript.tcl
+++ b/buildroot-external/package/openccu-base/rootfs-patches/0207-WebUI-Fix-ReGaScriptInjection/rootfs/www/api/eq3/hmscript.tcl
@@ -1,69 +1,98 @@
-##
-# hmscript.tcl
-# Ausführen von HomeMatic Script.
-#
-# Autor: Falk Werner
-##
-
-package require HomeMatic
-
-proc hmscript {script {p_args -}} {
-  set _script_ ""
-  
-  if { "-" != $p_args } then {
-    upvar $p_args args
-    
-    foreach name [array names args] {
-      append _script_ "var $name = \"[hmscript_escapeString $args($name)]\";\n"
-    }
-  }
-  
-  append _script_ $script
- 
-  return [hmscript_run _script_]  
-}
-
-##
-# Führt ein HomeMatic Script aus und liefert das Ergebnis
-##
-proc hmscript_run { p_script } {
-  upvar $p_script script
-  
-  if { ![catch {array set result [rega_script $script]}] } then {
-    return $result(STDOUT)
-  }
-  return ""
-}
-
-proc hmscript_runInline { script } {
-  if { ![catch {array set result [rega_script $script]}] } then {
-    return $result(STDOUT)
-  }
-  return ""    
-}
-
-proc hmscript_runFromFile { filename {p_args -}} {
-	set script ""
-	
-	if { "-" != $p_args } then {
-		upvar $p_args args
-    
-		foreach name [array names args] {
-			append script "var $name = \"[hmscript_escapeString $args($name)]\";\n"
-		}
-	}
-  append script [file_load $filename]
-  
-  return [hmscript_run script]
-}
-
-proc hmscript_escapeString { str } {
-  return [string map {
-    "\\" "\\\\"
-    "\'" "\\\'"
-    "\"" "\\\""
-    "\n" "\\n"
-    "\r" "\\r"
-    "\t" "\\t"
-  } $str]
-}
\ No newline at end of file
+##
+# hmscript.tcl
+# Ausführen von HomeMatic Script.
+#
+# Autor: Falk Werner
+##
+
+package require HomeMatic
+
+proc hmscript {script {p_args -}} {
+  set _script_ ""
+  
+  if { "-" != $p_args } then {
+    upvar $p_args args
+    
+    foreach name [array names args] {
+      set varname [hmscript_sanitizeIdentifier $name]
+      append _script_ "var $varname = \"[hmscript_escapeString $args($name)]\";\n"
+    }
+  }
+  
+  append _script_ $script
+ 
+  return [hmscript_run _script_]  
+}
+
+##
+# FÃ¼hrt ein HomeMatic Script aus und liefert das Ergebnis
+##
+proc hmscript_run { p_script } {
+  upvar $p_script script
+  
+  if { ![catch {array set result [rega_script $script]}] } then {
+    return $result(STDOUT)
+  }
+  return ""
+}
+
+proc hmscript_runInline { script } {
+  if { ![catch {array set result [rega_script $script]}] } then {
+    return $result(STDOUT)
+  }
+  return ""    
+}
+
+proc hmscript_runFromFile { filename {p_args -}} {
+	set script ""
+	
+	if { "-" != $p_args } then {
+		upvar $p_args args
+    
+		foreach name [array names args] {
+			set varname [hmscript_sanitizeIdentifier $name]
+			append script "var $varname = \"[hmscript_escapeString $args($name)]\";\n"
+		}
+	}
+  append script [file_load $filename]
+  
+  return [hmscript_run script]
+}
+
+proc hmscript_escapeString { str } {
+  return [string map {
+    "\\" "\\\\"
+    "\'" "\\\'"
+    "\"" "\\\""
+    "\n" "\\n"
+    "\r" "\\r"
+    "\t" "\\t"
+  } $str]
+}
+
+proc hmscript_assertFloat { value } {
+  if { ![string is double -strict $value] } then {
+    error "Value '$value' is not a valid float"
+  }
+}
+
+proc hmscript_assertInteger { value } {
+  if { ![string is integer -strict $value] } then {
+    error "Value '$value' is not a valid integer"
+  }
+}
+
+proc hmscript_assertBoolean { value } {
+  if { ![string is boolean -strict $value] } then {
+    error "Value '$value' is not a valid boolean"
+  }
+}
+
+# Strips characters not matched by sanitizeIdentifiers regex and fixes an invalid leading character.
+proc hmscript_sanitizeIdentifier { name } {
+  set replacements [regsub -all {[^A-Za-z0-9_]} $name {} newname ]
+  if { ![regexp {^[A-Za-z_]} $newname] } then {
+    set newname "_$newname"
+  }
+  return [string range $newname 0 63]
+}
diff --git a/buildroot-external/package/openccu-base/rootfs-patches/0207-WebUI-Fix-ReGaScriptInjection/rootfs/www/api/eq3/hmscript.tcl.orig b/buildroot-external/package/openccu-base/rootfs-patches/0207-WebUI-Fix-ReGaScriptInjection/rootfs/www/api/eq3/hmscript.tcl.orig
index 6d45346..613cc8a 100644
--- a/buildroot-external/package/openccu-base/rootfs-patches/0207-WebUI-Fix-ReGaScriptInjection/rootfs/www/api/eq3/hmscript.tcl.orig
+++ b/buildroot-external/package/openccu-base/rootfs-patches/0207-WebUI-Fix-ReGaScriptInjection/rootfs/www/api/eq3/hmscript.tcl.orig
@@ -1,68 +1,98 @@
-##
-# hmscript.tcl
-# Ausführen von HomeMatic Script.
-#
-# Autor: Falk Werner
-##
-
-package require HomeMatic
-
-proc hmscript {script {p_args -}} {
-  set _script_ ""
-  
-  if { "-" != $p_args } then {
-    upvar $p_args args
-    
-    foreach name [array names args] {
-      append _script_ "var $name = \"[hmscript_escapeString $args($name)]\";\n"
-    }
-  }
-  
-  append _script_ $script
- 
-  return [hmscript_run _script_]  
-}
-
-##
-# Führt ein HomeMatic Script aus und liefert das Ergebnis
-##
-proc hmscript_run { p_script } {
-  upvar $p_script script
-  
-  if { ![catch {array set result [rega_script $script]}] } then {
-    return $result(STDOUT)
-  }
-  return ""
-}
-
-proc hmscript_runInline { script } {
-  if { ![catch {array set result [rega_script $script]}] } then {
-    return $result(STDOUT)
-  }
-  return ""    
-}
-
-proc hmscript_runFromFile { filename {p_args -}} {
-	set script ""
-	
-	if { "-" != $p_args } then {
-		upvar $p_args args
-    
-		foreach name [array names args] {
-			append script "var $name = \"[hmscript_escapeString $args($name)]\";\n"
-		}
-	}
-  append script [file_load $filename]
-  
-  return [hmscript_run script]
-}
-
-proc hmscript_escapeString { str } {
-  return [string map {
-    "\'" "\\\'"
-    "\"" "\\\""
-    "\n" "\\n"
-    "\r" "\\r"
-    "\t" "\\t"
-  } $str]
-}
\ No newline at end of file
+##
+# hmscript.tcl
+# Ausführen von HomeMatic Script.
+#
+# Autor: Falk Werner
+##
+
+package require HomeMatic
+
+proc hmscript {script {p_args -}} {
+  set _script_ ""
+  
+  if { "-" != $p_args } then {
+    upvar $p_args args
+    
+    foreach name [array names args] {
+      set varname [hmscript_sanitizeIdentifier $name]
+      append _script_ "var $varname = \"[hmscript_escapeString $args($name)]\";\n"
+    }
+  }
+  
+  append _script_ $script
+ 
+  return [hmscript_run _script_]  
+}
+
+##
+# FÃ¼hrt ein HomeMatic Script aus und liefert das Ergebnis
+##
+proc hmscript_run { p_script } {
+  upvar $p_script script
+  
+  if { ![catch {array set result [rega_script $script]}] } then {
+    return $result(STDOUT)
+  }
+  return ""
+}
+
+proc hmscript_runInline { script } {
+  if { ![catch {array set result [rega_script $script]}] } then {
+    return $result(STDOUT)
+  }
+  return ""    
+}
+
+proc hmscript_runFromFile { filename {p_args -}} {
+	set script ""
+	
+	if { "-" != $p_args } then {
+		upvar $p_args args
+    
+		foreach name [array names args] {
+			set varname [hmscript_sanitizeIdentifier $name]
+			append script "var $varname = \"[hmscript_escapeString $args($name)]\";\n"
+		}
+	}
+  append script [file_load $filename]
+  
+  return [hmscript_run script]
+}
+
+proc hmscript_escapeString { str } {
+  return [string map {
+    "\\" "\\\\"
+    "\'" "\\\'"
+    "\"" "\\\""
+    "\n" "\\n"
+    "\r" "\\r"
+    "\t" "\\t"
+  } $str]
+}
+
+proc hmscript_assertFloat { value } {
+  if { ![string is double -strict $value] } then {
+    error "Value '$value' is not a valid float"
+  }
+}
+
+proc hmscript_assertInteger { value } {
+  if { ![string is integer -strict $value] } then {
+    error "Value '$value' is not a valid integer"
+  }
+}
+
+proc hmscript_assertBoolean { value } {
+  if { ![string is boolean -strict $value] } then {
+    error "Value '$value' is not a valid boolean"
+  }
+}
+
+# Strips characters not matched by sanitizeIdentifiers regex and fixes an invalid leading character.
+proc hmscript_sanitizeIdentifier { name } {
+  set replacements [regsub -all {[^A-Za-z0-9_]} $name {} newname ]
+  if { ![regexp {^[A-Za-z_]} $newname] } then {
+    set newname "_$newname"
+  }
+  return [string range $newname 0 63]
+}
-- 
2.55.0

